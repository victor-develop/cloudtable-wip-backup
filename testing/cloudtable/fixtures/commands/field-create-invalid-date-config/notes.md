# Field Create Invalid Date Config

Proves hardened `date.date` config validation rejects unsupported properties through
the normal `field.create` command path before event emission or receipt creation.
