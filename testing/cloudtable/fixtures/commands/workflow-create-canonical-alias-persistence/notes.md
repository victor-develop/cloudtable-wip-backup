# Workflow Create Canonical Alias Persistence

Proves the command bus fixture corpus accepts field-declared canonical workflow alias bindings when it rebuilds authoring metadata from persisted field config.
