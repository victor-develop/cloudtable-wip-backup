# Workflow Create Status Binding Mismatch

Proves generic `row.fields.status` condition bindings are rejected by the same metadata-driven validation path when the provided field id does not match the binding.
