# Workflow Create Status Binding Mismatch

Proves the persistence-backed command fixture executor still rejects generic `row.fields.status` field-id mismatches when hydrating binding metadata from seeded field config.
