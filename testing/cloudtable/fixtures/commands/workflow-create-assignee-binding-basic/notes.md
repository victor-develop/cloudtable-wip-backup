# Workflow Create Assignee Binding Basic

Proves the command transcript fixture corpus accepts field-declared canonical `row.assignee` condition bindings through the generic workflow authoring metadata path.
