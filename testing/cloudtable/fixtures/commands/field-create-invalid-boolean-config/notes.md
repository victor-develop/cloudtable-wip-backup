# Field Create Invalid Boolean Config

Proves hardened `boolean.checkbox` config validation rejects unsupported properties
through the normal `field.create` command path before event emission or receipt
creation.
